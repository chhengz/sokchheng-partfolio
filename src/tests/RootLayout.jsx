// RootLayout.js

const RootLayout = () => {
  return (
    <div>
      <header>{/* Your navigation bar */}</header>
      <main>{/* Outlet for nested routes */}</main>
    </div>
  );
};

export default RootLayout;
